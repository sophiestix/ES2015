'use strict'; //prevents making mistakes, it will throw an exception during run time

var student = { name: 'Ken' };
var student = { name: 'James' };

console.log(student);

const student = { name: 'Ken' };
var student = { name: 'James' }; // cannot assign to student the new value